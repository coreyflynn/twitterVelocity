{{#if height}}
<div id="{{ div_string }}" class="{{ span_class }}" style="height:{{ height }}px"></div>
{{else}}
<div id="{{ div_string }}" class="{{ span_class }}" style="height:120px"></div>
{{/if}}